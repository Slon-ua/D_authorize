{
  "name": "swapi",
  "version": "1.0.0",
  "description": "Project first",
  "scripts": {
    "api-tests_sand": "newman run collection.json  -d test.csv -e sand.json --reporters cli,junit --reporter-junit-export newman.xml",
    "api-tests_prod": "newman run collection.json  -d test.csv -e prod.json --reporters cli,junit --reporter-junit-export newman.xml"
  },
  "author": "Sergii Kokhan",
  "dependencies": {
    "newman": "^4.5.7"
  }
}
